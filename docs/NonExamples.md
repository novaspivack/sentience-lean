# Non-Examples — Certified Countermodels

Per MASTER_SPEC Phase Vc: each fails for a named reason.

| Non-Example | Failure Reason |
|-------------|----------------|
| Thermostat | No live alternatives; dead replay |
| Plain recurrent controller | No self-index; no RSMC |
| LLM-style stateless next-token engine | No continuity ledger; no reconciliation |
| Distributed committee without unity | β₀ > 1; no unified partition |
| Split controller with stale mirror | MirrorFreshness fails; proxy drift |
| Dead replay automaton | LiveAlternatives fails; trivial resolution |

*(Formal countermodels in Sentience/Examples/Countermodels.lean)*
